npm init -y 
npm install express 
npm install mysql
npm install ejs
npm install multer
npm install express-session
npm install cors

npm install body-parser


//引用套件
var express = require('express');
var app = express();
var connSql = require('./config');
let bp = require('body-parser');
let bpUrl = bp.urlencoded();
var session = require('express-session');
app.use(session({
  secret: 'abcde',
  resave:true,
  saveUninitialized:true,

  cookie:{
    path: '/',
    httpOnly: true,
    secure:false,
    maxAge:1000*1000
  }
}))
